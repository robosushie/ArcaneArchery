using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArrowManager : MonoBehaviour
{
    public int spellIdx;
    // Start is called before the first frame update
    public void SetSpellIdx(int x) { spellIdx = x; }
}
